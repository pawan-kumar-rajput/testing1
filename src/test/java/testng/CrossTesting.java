package testng;
import java.util.HashMap;
import java.util.Map;
import org.openqa.selenium.WebDriver;
public class CrossTesting {
	WebDriver driver;
	HashMap<String,WebDriver>hm=new HashMap<>(Map.of(
			
	));
	void setup() {	
	}
}
